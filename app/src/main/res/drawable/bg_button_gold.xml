<?xml version="1.0" encoding="utf-8"?>
<!--
  The sheet's print button. It used to inherit the dialog theme's background and
  come out as a black slab with a black label; here it is unmistakably the thing
  you press: brass plate, bright hairline, dark ink label.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#66FFFFFF">

    <item>
        <selector>
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <solid android:color="@color/accent_ghost" />
                    <corners android:radius="12dp" />
                    <stroke android:width="1dp" android:color="@color/accent_dim" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="270"
                        android:startColor="#E2C578"
                        android:endColor="#B08F45" />
                    <corners android:radius="12dp" />
                    <stroke android:width="1dp" android:color="@color/accent_soft" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
